# Deployment (consolidated)

Deployment docs have been consolidated to use AWS SAM. See infra/SAM-DEPLOY.md for local and CI deployment instructions (SAM build/package/deploy and OIDC guidance).

The previous Serverless and cfn-stack-based deployment files were removed; if you need to review their history, check the Git history for the removed files.
